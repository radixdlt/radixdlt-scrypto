[package]
name = "oracle_v2"
version = "1.0.0"
edition = "2021"

[dependencies]
scrypto = { path = "../../../../../scrypto" }

[dev-dependencies]
radix-engine = { path = "../../../../../radix-engine" }

[features]
default = []
test = []

[lib]
doctest = false
crate-type = ["cdylib", "lib"]
