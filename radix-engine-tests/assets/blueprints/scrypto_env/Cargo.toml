[package]
name = "scrypto_env"
version = "1.2.0-dev"
edition = "2021"

[dependencies]
scrypto = { path = "../../../../scrypto" }

[dev-dependencies]
radix-engine = { path = "../../../../radix-engine" }

[lib]
doctest = false
crate-type = ["cdylib", "lib"]