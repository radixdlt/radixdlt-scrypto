name: 'Setup environment'
description: 'Common GH action to setup job environment'

runs:
  using: "composite"
  steps:
    - name: Install Rust toolchain
      uses: actions-rs/toolchain@v1
      with:
        toolchain: stable
        default: true
        target: wasm32-unknown-unknown

    - name: Install nextest
      uses: taiki-e/install-action@nextest

    - name: Set LIBCLANG_PATH # See https://github.com/rust-lang/rust-bindgen/issues/1797
      if: runner.os == 'Windows'
      run: echo "LIBCLANG_PATH=$((gcm clang).source -replace "clang.exe")" >> $env:GITHUB_ENV
      shell: bash
    - name: Install dependencies
      if: runner.os == 'Windows'
      run: choco install llvm -y
      shell: bash
    - name: Setup cmake
      if: runner.os == 'Linux'
      uses: jwlawson/actions-setup-cmake@v1.13
      with:
        cmake-version: '3.27.9'
    - name: Install clang
      if: runner.os == 'Linux'
      run: sudo apt-get install clang libclang-dev -y -f
      shell: bash
    - name: Install clang
      if: runner.os == 'macOS'
      run: |
        brew install llvm
        echo $PATH
        /usr/local/opt/llvm/bin/clang -v
        /usr/local/opt/llvm/bin/clang --print-targets
        echo 'export PATH="/usr/local/opt/llvm/bin:$PATH"' >> /Users/runner/.bash_profile

      shell: bash

