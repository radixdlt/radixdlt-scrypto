name: CI

on:
  push:
    branches:
      # Temp thing so that this only runs on this branch
      - feature/coverage

env:
  CARGO_TERM_COLOR: always

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  sbor-unit-tests:
    name: Run SBOR unit tests
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [k8s-linux-runner]
    steps:
      - uses: radixdlt/public-iac-resuable-artifacts/rust-coverage.yml@feature/rust-coverage
        with:
          tests-dir: sbor
