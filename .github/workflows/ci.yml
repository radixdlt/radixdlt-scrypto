name: CI

on:
  push:
    branches:

env:
  CARGO_TERM_COLOR: always

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  radix-engine:
    name: Run Radix Engine tests
    uses: radixdlt/public-iac-resuable-artifacts/.github/workflows/rust-tests.yml@feature/rust-coverage-workflow
    with:
      flags: -p radix-engine-common -p radix-engine-derive -p radix-engine-interface -p radix-engine
