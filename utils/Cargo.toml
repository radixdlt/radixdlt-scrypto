[package]
name = "utils"
version = "0.7.0"
edition = "2021"

[dependencies]
sbor = { path = "../sbor", default-features = false }

[features]
default = ["std"]
std = ["sbor/std"]
alloc = ["sbor/alloc"]
