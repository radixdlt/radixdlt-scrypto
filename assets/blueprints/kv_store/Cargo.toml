[package]
name = "kv_store"
version = "0.1.0"
edition = "2021"

[dependencies]
sbor = { path = "../../../sbor" }
scrypto = { path = "../../../scrypto" }

[dev-dependencies]
scrypto-test = { path = "../../../scrypto-test" }

[lib]
crate-type = ["cdylib", "lib"]