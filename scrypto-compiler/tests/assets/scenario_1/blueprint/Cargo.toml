[package]
name = "test_blueprint"
version = "1.1.0"
edition = "2021"

[dependencies]
scrypto = { path = "../../../../../scrypto" }

[lib]
crate-type = ["cdylib", "lib"]

[features]
feature-1 = []

[package.metadata.scrypto]
