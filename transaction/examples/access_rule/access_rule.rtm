UPDATE_ROLE
    Address("${resource_address}")
    Enum<0u8>()
    "hello"     # The name of the role to update the access rule for.
    None        # The rule associated with the role, can either be Some `AccessRule` or none.
    false;      # Whether to freeze the role