use crate::types::*;
use radix_engine_interface::blueprints::resource::{AccessRule, GroupEntry, MethodKey};

#[derive(ScryptoSbor, ScryptoEvent)]
pub enum SetRuleEvent {
    Method {
        key: MethodKey,
        rule: GroupEntry,
    },
    Group {
        name: String,
        rule: GroupEntry,
    },
}

#[derive(ScryptoSbor, ScryptoEvent)]
pub enum SetMutabilityEvent {
    Method {
        key: MethodKey,
        mutability: GroupEntry,
    },
    Group {
        name: String,
        mutability: AccessRule,
    },
}
