use crate::types::*;
use radix_engine_interface::blueprints::resource::{AccessRule, MethodKey};

#[derive(ScryptoSbor, ScryptoEvent)]
pub enum SetRuleEvent {
    Method { key: MethodKey, rule: AccessRule },
    Group { name: String, rule: AccessRule },
}

#[derive(ScryptoSbor, ScryptoEvent)]
pub enum SetMutabilityEvent {
    Method {
        key: MethodKey,
        mutability: AccessRule,
    },
    Group {
        name: String,
        mutability: AccessRule,
    },
}
