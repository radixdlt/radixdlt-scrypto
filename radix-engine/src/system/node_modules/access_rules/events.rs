use crate::types::*;
use radix_engine_interface::blueprints::resource::AccessRule;

#[derive(ScryptoSbor, ScryptoEvent)]
pub struct UpdateRoleEvent {
    pub role_key: RoleKey,
    pub rule: Option<AccessRule>,
    pub freeze: bool,
}

#[derive(ScryptoSbor, ScryptoEvent)]
pub struct UpdateOwnerRoleEvent {
    pub rule: Option<AccessRule>,
    pub freeze: bool,
}
